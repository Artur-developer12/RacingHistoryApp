import { Theme } from '@emotion/react'

const theme: Theme = {
  colors: {
    common: {
      white: '#ffffff',
      whiteTransparent: 'rgba(255, 255, 255, 0.2)',
      black: '#000000',
      blackTransparent: 'rgba(0, 0, 0, 0.08)',
    },
  },
}

export default theme
